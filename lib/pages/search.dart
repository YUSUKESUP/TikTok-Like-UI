import 'package:flutter/material.dart';

class UserSerchPage extends StatelessWidget {
  const UserSerchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Search')),
    );
  }
}
